# Railway configuration - use with Dockerfile
[build]
# Railway will auto-detect Dockerfile

[deploy]
# Auto-configured by Dockerfile CMD

[env]
PYTHONUNBUFFERED = "1"
STREAMLIT_SERVER_HEADLESS = "true"
STREAMLIT_SERVER_ENABLE_XSRF_PROTECTION = "true"
